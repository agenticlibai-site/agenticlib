name: Marketing Feature Scoring (Claude)

on:
  schedule:
    - cron: '30 8 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Trigger marketing feature scoring
        run: |
          curl --max-time 310 --fail-with-body \
            "https://agenticlib.com/api/cron/feature-scoring-collection?model=claude-haiku-4-5" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
