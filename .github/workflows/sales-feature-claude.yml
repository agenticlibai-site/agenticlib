name: Sales Feature Collection (Claude)

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 24
    steps:
      - name: Trigger sales feature collection (quarter 1)
        run: |
          curl --max-time 310 --fail-with-body \
            "https://agenticlib.com/api/cron/sales-feature-collection?model=claude-haiku-4-5&quarter=1" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
      - name: Trigger sales feature collection (quarter 2)
        if: always()
        run: |
          curl --max-time 310 --fail-with-body \
            "https://agenticlib.com/api/cron/sales-feature-collection?model=claude-haiku-4-5&quarter=2" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
      - name: Trigger sales feature collection (quarter 3)
        if: always()
        run: |
          curl --max-time 310 --fail-with-body \
            "https://agenticlib.com/api/cron/sales-feature-collection?model=claude-haiku-4-5&quarter=3" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
      - name: Trigger sales feature collection (quarter 4)
        if: always()
        run: |
          curl --max-time 310 --fail-with-body \
            "https://agenticlib.com/api/cron/sales-feature-collection?model=claude-haiku-4-5&quarter=4" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
