name: Sales Sentiment Collection (Claude)

on:
  schedule:
    - cron: '0 11 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Trigger sales sentiment collection
        run: |
          curl --max-time 310 --fail-with-body \
            "https://agenticlib.com/api/cron/sales-sentiment-collection?model=claude-haiku-4-5" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
