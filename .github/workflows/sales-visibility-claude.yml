name: Sales Visibility Collection (Claude)

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Trigger sales visibility collection
        run: |
          curl --max-time 310 --fail-with-body \
            "https://agenticlib.com/api/cron/sales-visibility-collection?model=claude-haiku-4-5" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
